from cProfile import label
from tkinter import *
root = Tk()
def maytinh() :
    temp = int(entry.get())
    temp = 9/5*temp+32
    output_label
message_label = Label(text="Enter a Temparture",font=('Verdana',16))
output_label = Label(font=('Verdana',16))
entry = Entry(font=('Verdana',16),width=5)
calc_but = Button(text='OK',font=('Verdana',16),command=maytinh)
me
mainloop()